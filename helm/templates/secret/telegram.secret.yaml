{{- if not .Values.telegram.secret }}
apiVersion: v1
kind: Secret
metadata:
  labels:
    {{ include "helm.labels" . | nindent 4 }}
  name: {{ include "helm.fullname" . }}-telegram
data:
  TELEGRAM_BOT_TOKEN: '{{ .Values.telegram.token }}'
{{- end }}
