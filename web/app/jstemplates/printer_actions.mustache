<div class="row">
{{#status}}
    {{#error}}
    <div class="col-12 text-danger text-center">
    OctoPrint ran into an error!
    </div>
    {{/error}}
    {{^disconnected}}
        {{^idle}}
            <div class="col-sm-6">
                <button id="print-pause-resume" type="button"
                    class="btn {{^printerPaused}}btn-warning{{/printerPaused}} {{#printerPaused}}btn-success{{/printerPaused}} btn-block mt-2" >
                <span>
                {{^printerPaused}}Pause{{/printerPaused}} {{#printerPaused}}Resume{{/printerPaused}}
                </span>
                    {{#alertShowing}}
                    <img class="dh-icon"
                    src="{{dhInverseIconSrc}}" />
                    {{/alertShowing}}
                </button>
            </div>
            <div class="col-sm-6">
                <button id="print-cancel" type="button" class="btn btn-danger btn-block mt-2 mb-2" >
                <span>Cancel
                </span>
                    {{#alertShowing}}
                    <img class="dh-icon"
                    src="{{dhInverseIconSrc}}" />
                    {{/alertShowing}}
                </button>
            </div>
        {{/idle}}
        {{#idle}}
            <div class="col-12 text-center py-3">
            Printer is idle.
            <button id="start-print" type="button" class="btn btn-primary btn-block mt-2">
            Send G-Code and Start Printing
            </button>
            </div>
        {{/idle}}
    {{/disconnected}}
    {{#disconnected}}
        <div class="col-12 text-center py-2 text-warning">
            <div>Printer is not connected to OctoPrint.</div>
            <button id="connect-printer" type="button" class="btn btn-success btn-block mt-2">
            Connect <i class="fas fa-spinner fa-spin" style="display: none;"></i>
            </button>
        </div>
    {{/disconnected}}
{{/status}}
{{^status}}
    <div class="col-12 text-center py-3 text-warning">
    <div>OctoPrint is not powered on, or is offline. <a href="https://www.thespaghettidetective.com/docs/octoprint-is-offline/">Why?</a></div>
    </div>
{{/status}}
</div>
